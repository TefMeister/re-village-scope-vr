# 2026-09-21 14:03:49 — the wash, timed to the second

Tefa's headset capture `wash-1403-49.jpg`: the grey-blue wash fills the right half of the glass,
split from the picture by a straight vertical edge.

**The plugin log at that second** (`plugin-log-1402-1404.txt`): the last two normal
`crop-follow`/`geom` pairs are at 14:03:45.6 and 14:03:46.8. From **14:03:47.755 onward, every line
reads `crop-follow: no centre this tick (bore=0 root=1 fov=81.1) -> HOLDING the last good centre`**
until at least 14:03:59 `[verified-live 2026-09-21, n=1 timed sighting]`. So the wash coincides with
the verified muzzle axis being LOST (`bore=0`) while the crop is held at its last centre. Whether
the loss CAUSES the wash, or both come from one upstream event, is not established `[hypothesis]`.
The earlier (13:05:56) sighting's lines in `../2026-09-21-roll-disproved-two-hand-snap/` should be
re-read with this in mind. `framework-log-1402-1404.txt` has the Lua side for the same window.
