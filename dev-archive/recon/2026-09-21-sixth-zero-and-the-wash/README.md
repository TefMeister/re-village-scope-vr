# 2026-09-21 13:53 — the sixth zero, and the wash back on the right

Tefa zeroed FAR with distance-follow ON and the look-back at 3 (dossier §9cd): **up −12.1 /
right −8.0** `[verified-live 2026-09-21, n=1 wearer]`. It was saved to the harness defaults,
the settings file, `ZERO-B-SHIPPED.bat` and `READ-ME-ZEROING.txt`.

The grey-blue wash was back on the right of the glass `[reported 2026-09-21]`.
`geom-lines.txt` has every `geom:` line from 13:40 to 13:59. In **20 of 163** the crop centre
lies outside the render (u > 1 or v < 0, e.g. `(1.038,0.268)`, `(0.750,-0.649)` with stretch
2.07). That is a lead for the wash, not a finding: those lines may fall while the rifle is
moving or lowered, and nothing here times them against the moment the wash was seen
`[hypothesis]`.
