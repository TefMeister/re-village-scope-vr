# Research index

**Last `/gr` pass: 2026-09-04 (estate sweep, second pass today) — CHECK-IN** (INDEX; one cross-project gap, reached from the sibling RE Engine project)**.** Inbox empty, nothing searched for this project directly. **One gap found and dropped to the modding lane:** this project records no REFramework revision anywhere, which on this framework makes every Lua result a statement about two programs. Reading the framework's own startup log answers it with no launch.

_Previous: **Last `/gr` pass: 2026-09-04 (estate sweep) — CHECK-IN** (inbox drained — one modding verdict; INDEX; the tonemap topic it answers)**.** The 2026-09-03 tonemap lead → ✅ **built the same day** (`staging 87efe59`, compile-verified, first look is a `[FLAT]` row). Two corrections folded in: the linear section ends at **0.532**, not 0.62 (GT's `l` is a fraction of the headroom over the contrast, not an absolute length), and the GT identification is upgraded to `[inferred-static]` because the four live values are Uchimura's published defaults to the digit. Expectation reset: GT alone clips harder at the same knob; the win is that the knob can come down. **No new research** — every remaining row is a launch or a headset check._
_Previous: **Last `/gr` pass: 2026-09-03 (estate sweep) — FULL** (board OPEN block, INDEX, dossier and the 2026-08-30 grading recon read)**.** Inbox empty. One new topic, on the `[PD]` tonemap-shoulder row — and the answer was in **our own recon**, not on the web: `via.render.ToneMapping` exposes the game's whole curve and the live values were dumped on 2026-08-30 (`UseTripleSectionTonemap=true`, `LinearSectionBegin=0.22`, `LinearSectionLength=0.40`, toes, white points), with setters as well as getters. ⭐ The insight: **`1-exp(-x·e)` has no linear section and the game's curve is linear from 0.22 to 0.62** — so the missing shoulder and the fitted `exposure=0.134` are the *same* defect, and a three-section curve fixes both at once while preserving the midtone calibration by construction. Curve identified as GT/Uchimura-shaped `[hypothesis]`, but the plan does not depend on that. Also flagged: the game moves `MinWhitePoint`/`WhiteRange` at runtime, so read them per frame like `get_EV`.
_Previous: **Last `/gr` pass: 2026-09-02 (estate sweep) — CHECK-IN** (board OPEN block + INDEX; no dossier or topic re-read)**.** Inbox empty. **Nothing new.** Open rows are a never-run virtual numpad, an in-headset retune and a hand-off only the user can send; the stand…_

Every research topic gathered for this project, newest first. Each row links to a self-contained
write-up in `topics/`. Status tags:

- 🆕 **new** — found, not yet acted on by the modding side.
- 👀 **reviewed** — a modding session has read it and factored it into a decision, but nothing shipped from it yet.
- ✅ **incorporated** — directly led to a real change (code, a test, a note) in one of the other five repos; linked below.
- ❌ **dead end** — checked out, didn't pan out; kept for the record so it isn't re-investigated from scratch.

| Date | Topic | Status | Summary |
| --- | --- | --- | --- |
| 2026-09-04 | [This project records no REFramework revision](../engine-research/inbox/2026-09-04-gr-this-project-records-no-reframework-revision.md) (drop for the modding lane) | 🆕 new | **A gap, not a finding about the game.** Neither the dossier nor the board records the REFramework build date, branch or hash `[verified-live 2026-09-04, n=1 grep]`, so every Lua result here is dated while the program under it is not. On this framework that bites: the release is eighteen months behind `master` `[reported 2026-09-04]`, a documented callback contract silently failed for nine days (PR #1503 → #1809), and September brought Lua array / string-number fixes that exist only on `master`. The sibling RE Engine project cleared itself of the callback bug in minutes **because it had a revision to date-check**. No launch needed — REFramework prints its branch and version to its log at startup. ⚠️ Do not copy the sibling's `pd-upscaler` pin; that is specific to its DLSS requirement. |
| 2026-09-03 | [The game's own tonemap curve is already dumped — and `1-exp` has no linear section](topics/2026-09-03-the-games-own-tonemap-curve-is-already-dumped-and-it-has-a-linear-section.md) | ✅ incorporated — built 2026-09-03 (`staging 87efe59`, compile-verified, first look is a `[FLAT]` row) | **⭐ Answers the `[PD]` highlight-shoulder row, from our own 2026-08-30 recon rather than the web.** `via.render.ToneMapping` exposes the whole curve with live values already captured — `UseTripleSectionTonemap=true`, `LinearSectionBegin=0.22`, `LinearSectionLength=0.40`, `SDRToe=1.0`/`HDRToe=1.33`, `MinWhitePoint=5.6`/`MaxWhitePoint=15.0`/`WhiteRange=0.9` — and every one has a setter too. The key point: **`1-exp(-x·e)` curves everywhere, while the game is dead straight from 0.22 to **0.532** (corrected by the modding lane; the topic first said 0.62)**, so the clipped highlights and the hand-fitted `exposure=0.134` are one defect, not two; a triple-section curve reproduces the midtones exactly (they *are* the linear band) and rolls off separately, with no trade-off to tune. Don't reach for Hable/ACES — copy the game, as the project already does for EV. Shape matches the GT/Uchimura curve — upgraded to `[inferred-static 2026-09-03]`: the four live values are Uchimura's published defaults to the digit. Expectation reset: GT alone clips harder at the *same* knob; the win is that the knob can come down without the midtones collapsing. ⚠️ Read the params per frame — the log shows the game moving `MinWhitePoint` 5.6→8.0 and `WhiteRange` 0.9→0.8 mid-session, so a hardcoded shoulder would silently drift. |
| 2026-08-29 | [Runtime mesh spawning: via.Prefab + instantiate](topics/2026-08-29-runtime-mesh-spawning-via-prefab-instantiate.md) | ✅ incorporated — staging `c4052d3`, P-series buttons P0–P4 (2026-08-29) | Answers the M18–M26 wall: don't assemble GameObjects from components — instantiate a `.pfb` prefab (`via.Prefab` → `set_Path` → `get_Exist` → `instantiate(via.vec3, via.Folder)`); EMV Engine's proven route, RE8 explicitly supported, and EMV's own README warns component assembly is fragile. RE8 ships 2,670 prefabs incl. `ri3042_detailsearch.pfb` (a spawnable copy of our rifle) and `c22e500_00_mirror.pfb` (a Capcom-assembled cutscene mirror to read the recipe off). Unlocks the pane-steering test that all three mirror limits hang on. |
| 2026-08-24 | [RT GPU-backing: known REResource bug + VR eye-texture architecture](topics/2026-08-24-rt-gpu-backing-known-reresource-bug-and-vr-eye-texture-architecture.md) | 🆕 new | REFramework issue #1448 describes an unresolved resource-lifetime/GC bug matching the "RT binds but shows nothing" symptom — workaround: `add_ref()` both the holder AND the resource, create early. Also: REFramework's own VR eye texture is a post-render backbuffer copy, not engine-side scene→RT registration — doesn't transfer, but reinforces that Mirror-as-producer (candidate 1) is the right direction, not a backbuffer-copy shortcut. Still no public in-world-screen/second-camera RE Engine mod found. |

## How to add a topic

1. New file in `topics/`, named `YYYY-MM-DD-short-slug.md`.
2. One row added to the table above, newest at the top.
3. Update the status tag here as it moves through review → incorporated/dead-end (the modding side should update this when it acts on a lead, so the index reflects reality without the research side needing to poll).
